[book]
authors = ["Jho00"]
language = "en"
multilingual = false
src = "src"
title = "system-engineering-playbook"

[build]
build-dir = "book"
create-missing = true

# [preprocessor.drawio]
# command = "mdbook-drawio"

[output.html]
# additional-css = ["_assets/custom.css"]
additional-js = []
# Mermaid will be loaded via inline script in the HTML

# Для отображения Mermaid диаграмм установите mdbook-mermaid:
# cargo install mdbook-mermaid
# Затем раскомментируйте следующую строку:
# [preprocessor.mermaid]
# command = "mdbook-mermaid"
# 
# Примечание: На GitHub Pages preprocessor включен автоматически через GitHub Actions

[output.html.playground]
editable = false